# Deprecated Experiments

This directory contains historical scaffolds only. Never submit a Slurm script
or reuse an output directory from here for a new result.

The active experiments are direct children of `experiments/` and each has one
scientific hypothesis, immutable config, isolated output paths, and its own
launcher.
